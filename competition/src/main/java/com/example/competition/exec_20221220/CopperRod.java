package com.example.competition.exec_20221220;

/**
 * 有3根铜条，长度分别为X、Y、Z，现在要从中截取1000根长度相同的小铜棒，请写个函数计算可以截取的小铜棒的最大长度（提示：通过计算机的计算能力解决问题，不要试图直接用数学方法解决）。
 * 函数定义：
 * double getMaxLen(double X, double Y, double Z)
 */
public class CopperRod {

    public static void main(String[] args) {
        double maxLen = getMaxLen(5, 15, 20);
        System.out.println(maxLen);
    }

    public static double getMaxLen(double x, double y, double z) {
        // 假设铜棒的最大长度为 a
        double maxLen = 0d;

        return maxLen;
    }
}
